[project]
name = "v2t-single"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "google-genai>=0.8.0",
    "langgraph>=0.2.0",
    "pyyaml>=6.0",
    "langsmith>=0.1.0",
    "python-dotenv>=1.0.0",
    "pydantic>=2.7.0",
]

[dependency-groups]
dev = [
    "langgraph-cli[inmem]>=0.4.26",
]
